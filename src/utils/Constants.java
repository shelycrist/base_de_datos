package utils;

public class Constants {
    public static enum prioridadEnum { alta, media, baja };
    public static enum tipoAyudaEnum { medica, donacion };
    public static enum estadoEnum { aprobado, negado, pendiente };
}
